package com.jobtracker.entity;

/**
 * Defines the types of users in the system.
 */
public enum UserRole {
    JOB_SEEKER, // Individual looking for work
    COMPANY, // Organization posting jobs
    ADMIN // System operator
}
